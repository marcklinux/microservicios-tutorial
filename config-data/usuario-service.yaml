server:
  port: 7002
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:7005/eureka
    instance:
      hostname: localhost